# Tasks — 005 Announcing that a version was published

Derived from [plan.md](./plan.md). Task ids are stable and are what `feature-state.json` tracks.
Every task follows Article I: the failing test comes first.

Documentation is not batched into a closing story. Each public name is documented in the story that
introduces it, so the docs check is a real signal at every story boundary.

## Foundational — none

Everything this feature builds on is on main from FS-001 to FS-004. The stories run in sequence:
US-2, then US-1, then US-3 (plan.md, *Stories and order*).

## US-2 — A version records who published it (P1, issue #45)

| Id | Task | Done when |
|---|---|---|
| T001 | `tests/test_models.py::TestPublisher` — publishing a draft with `publisher=user` records the user on `publisher` and `str(user.pk)` on `publisher_subject`; publishing with no publisher leaves both empty; a draft has neither (scenarios 3, 7, FR-008, FR-011) | Fails for the right reason before T003 |
| T002 | `tests/test_models.py::TestPublisher` — on a published version, changing `publisher` or `publisher_subject` through `save()` raises `PublishedVersionError`, and through `Version.objects.filter(...).update(publisher=..)`, `update(publisher_id=..)` and `update(publisher_subject=..)` raises too; creating a draft that names a publisher is refused by the database (scenario 2, FR-009, SC-004) | Fails before T003 |
| T003 | `mvp_compliance/models.py` — the two fields per plan.md *The publisher*, both in `PUBLISHED_FROZEN_FIELDS`; the draft branch of `version_status_agrees_with_its_publication` widened; `publish(publisher=None)` writing both through `Acceptance.subject_of()`, whose docstring and unsaved-user message widen to cover a publisher. The frozen-field refusal message stops saying "wording" only (it now covers the publisher). Migration `0005_version_publisher.py` generated by `makemigrations` | T001–T002 pass; `makemigrations --check` clean |
| T004 | `tests/test_models.py::TestPublisher` — removing the publisher's account succeeds, the version is otherwise unchanged (compare every other frozen field before and after), `publisher` is null and `publisher_subject` is kept (scenario 6, FR-013, SC-005) | Passes; probe recorded: with `Meta.base_manager_name = "objects"` set temporarily the test fails, reverted |
| T005 | `tests/test_models.py::TestPublisherDisplay` + `Version.publisher_display` — the four rows of the plan's table, including a version whose account was removed reading "An account since removed" and never containing the old subject, and a pre-existing published version (both fields empty) reading "No publisher recorded" (scenarios 5, 6, 7, edge cases) | Passes |
| T006 | `tests/test_migrations.py` — migrating a database holding a published version from `0004` to `0005` keeps the version intact, with `publisher` null and `publisher_subject` empty (Article XVI, scenario 5) | Passes |
| T007 | `tests/test_admin.py` — publishing through the publish page as a user with `publish_version` records that user as the publisher (scenario 1, FR-010) | Fails before T008 |
| T008 | `mvp_compliance/admin.py` — `publish_view` passes `publisher=request.user` | T007 passes |
| T009 | `tests/test_admin.py` — a published version's page shows *Published by* with the publisher next to *Published at*; the version list and the documents list show the publisher column; a draft's page shows the empty value; the documents list and version list query counts do not grow with the row count (scenario 4, FR-012) | Fails before T010 |
| T010 | `mvp_compliance/admin.py` — `published_by` on `VersionAdmin` (`readonly_fields`, `list_display`, `list_select_related`) and `DocumentAdmin` (`list_display`, `select_related("publisher")` inside the prefetch) | T009 passes |
| T011 | `CONTEXT.md` **Publisher** (FR-024); `docs/models.md` *Publishing* (the argument, the two fields, what `publisher_display` says) and its list of frozen fields; `docs/authoring.md` *Publishing* and its list of what never changes (the admin records who published and shows it); `CHANGELOG.md` `[Unreleased]` entry naming the new personal data in plain language (Article XV); `en` catalog regenerated | Docs check green on the branch |

## US-1 — A project hears about every publication (P1, issue #44)

| Id | Task | Done when |
|---|---|---|
| T012 | `tests/test_models.py::TestVersionPublished` — a connected receiver runs exactly once per publication, with `sender=Version`, `version`, `publisher` and `replaced`; a document's first publication passes `replaced=None`; `replaced` arrives with the superseded standing (scenarios 1–3, FR-001, FR-002) | Fails before T013 |
| T013 | `mvp_compliance/signals.py` with `version_published` and its argument comment; `publish()` registers `send_robust` with `transaction.on_commit` after `save()`, inside the atomic block; `current.status` set to superseded on the instance | T012 passes |
| T014 | `tests/test_models.py::TestVersionPublished` — with callbacks executed (`django_capture_on_commit_callbacks(execute=True)`), for each of the three refusals (already published, empty once rendered, same wording as the version in force) the receiver never runs, paired in the same test with a control publication that does run it (scenario 4, FR-003, SC-001) | Passes |
| T015 | `tests/test_models.py::TestVersionPublished` — a publication inside `transaction.atomic()` that then raises produces no call (scenario 5, FR-004, SC-002); on commit, the receiver's own query sees the version as current and the replaced one as superseded (scenario 6). Uses `transactional_db` or `django_capture_on_commit_callbacks` as each case needs | Passes |
| T016 | `tests/test_models.py::TestVersionPublished` — with callbacks executed, a receiver that raises: `publish()` returns normally, the version is current, the error is logged on `django.dispatch`, and a second receiver connected after it still runs (scenario 8, FR-005); a receiver that publishes another version announces that one too (edge case); two documents published in one transaction are each announced once after it commits (edge case) | Passes |
| T017 | `tests/test_admin.py` — with callbacks executed and a receiver that raises connected, publishing through the admin leaves the version current, redirects to its page, shows a success message and logs the error (scenario 7, FR-006, SC-003) | Fails before T018 |
| T018 | `mvp_compliance/admin.py` — `publish_view` adds a success message after a publication that succeeded | T017 passes |
| T019 | `docs/announcing.md` (new): when the signal is sent and when it is not, every argument it carries (FR-023), what happens when a receiver fails, including that errors are logged on the `django.dispatch` logger at ERROR and that Django's default logging shows them only with DEBUG on or ADMINS set, so a project relying on a receiver routes that logger; that receivers run synchronously in the publishing request after the commit, so slow work belongs on a task queue; that the package sends nothing itself (FR-007). README documentation list links it; `CHANGELOG.md` Added entry; `en` catalog regenerated | Docs check green on the branch |

## US-3 — A ready-made email for the people running the site (P2, issue #46)

| Id | Task | Done when |
|---|---|---|
| T020 | `tests/test_emails.py::TestRenderPublicationEmail` — returns `(subject, body)` and sends nothing (`mail.outbox` empty); the body names the document, the version number, `publisher_display`, the moment and the replaced version's number; a first publication says it is the first version in force; no publisher recorded says so; the body carries `site_url` + the admin change URL for the version (scenarios 1–5, FR-016, FR-017, FR-018) | Fails before T021 |
| T021 | `mvp_compliance/emails.py` and the two templates per plan.md *The email* | T020 passes |
| T022 | `tests/test_emails.py::TestRenderPublicationEmail` — a document named with a line break and markup-like characters gives a one-line subject and a body carrying the name as written, unescaped (scenario 8, FR-019, SC-007, edge case) | Passes |
| T023 | `tests/test_emails.py::TestRenderPublicationEmail` — a template at the same path in a test-only templates directory replaces the package's (scenario 6, FR-021) | Passes |
| T024 | `tests/locale/de/LC_MESSAGES/django.po` + compiled `.mo` for the email strings, `LOCALE_PATHS` in `tests/settings.py`; test that under `translation.override("de")` the subject and body come back in that language (scenario 7, FR-020) | Passes |
| T025 | `docs/announcing.md` gains the email: the function, the receiver example from plan.md, overriding the templates by path; send the body as plain text, never as `html_message`, because names in it are unescaped; `CHANGELOG.md` Added entry; `en` catalog regenerated, with a test that every string in both templates is in it (SC-008) | Docs check green; catalog test passes |
