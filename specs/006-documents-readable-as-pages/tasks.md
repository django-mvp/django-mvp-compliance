# Tasks — 006 Documents and their versions readable as pages of the site

Derived from [plan.md](./plan.md). Task ids are stable and are what `feature-state.json` tracks.
Every task follows Article I: the failing test comes first.

Documentation is not batched into a closing story. Each public name is documented in the story that
introduces it, so the docs check is a real signal at every story boundary.

## Foundational — none

Everything this feature builds on is on main from FS-001 to FS-005. The stories run in sequence:
US-1, US-2, US-3, US-4 (plan.md, *Stories and order*).

## US-1 — Reading the document in force (P1, issue #60)

| Id | Task | Done when |
|---|---|---|
| T001 | `tests/test_models.py::TestDocumentSlug` — a document saves with a slug; two documents with the same slug are refused by the database. `DocumentFactory` gains a slug sequence. `tests/test_models.py::TestDocumentInForce` — `Document.objects.in_force()` returns documents with a current version, each carrying it on `current_versions`, and excludes one with only drafts and one with no versions (FR-007). The existing tests in `tests/test_models.py::TestDocument` that build documents inline get a distinct `slug=` each, with their assertions unchanged, and `test_duplicate_name_is_refused` gives the second document a different slug so it still proves the name constraint rather than the slug one (decisions.md D2) | Fails before T002 |
| T002 | `mvp_compliance/models.py` — `Document.slug` per plan.md *The slug* (field only, no validator or freeze yet: US-4), `DocumentQuerySet.in_force()` with a `DocumentManager.in_force()` forwarder, matching `outstanding_for()`. Migration `0007_document_slug.py` generated by `makemigrations`. `seed_demo` gets `get_or_create(name=..., defaults={"slug": ...})` with the slugs from plan.md, so no task boundary leaves the demo broken. Any existing migration test that builds documents at an earlier state still passes | T001 passes and the full existing suite is green; `makemigrations --check` clean |
| T003 | `tests/settings.py` gains `FLEX_MENUS` renderers; `tests/urls.py` mounts `mvp_compliance.urls` under `legal/`. `tests/test_views.py::TestDocumentView` — anonymous GET of the document's address is 200 and contains the current version's stored `html` byte for byte, and still renders with `mvp_compliance.rendering.MarkdownRenderer.render` patched to raise, which proves nothing renders Markdown at request time; the page names the document, the version number and the date in force; the breadcrumb trail links the document to `mvp_compliance:index`'s address once it exists (US-3) and until then carries no empty link; the response uses the shell (a marker unique to mvp's `base.html`) with no project template; after a second version is published the same address shows it (scenarios 1–5, FR-001, FR-006, FR-009, FR-012, SC-002, SC-004) | Fails before T004 |
| T004 | `mvp_compliance/urls.py` (new, `app_name`, `document` pattern), `mvp_compliance/views.py` (new, `DocumentView` per plan.md *Views*), `templates/mvp_compliance/document_detail.html` (without the version-list link: US-3) | T003 passes |
| T005 | `tests/test_views.py::TestDocumentView` — draft-only document, unknown slug: 404 for anonymous, a signed-in user and staff (scenarios 6–8, FR-007, FR-008, SC-003); a document name with markup-like characters is escaped (edge case); query count for the page is the same with one and with five published versions (SC-006); no link or button to edit or delete appears | Passes |
| T006 | Demo: `demo/settings.py` `FLEX_MENUS` + `MVP_CONFIG` sidebar title, `demo/urls.py` mounts `legal/`, landing page links the document pages. `python manage.py check` clean; `seed_demo` runs on a fresh database | Demo serves `/legal/privacy-policy/` with status 200 |
| T007 | `docs/pages.md` (new): mounting, the `document` URL name, linking from a footer, what the page shows and when it is "not found", and that the stored HTML is written out as the configured renderer sanitised it, so a project that points `MVP_COMPLIANCE_RENDERER` at a looser allow list serves that looseness on its own pages. `docs/models.md`: the `slug` field and `in_force()`. README documentation list links `docs/pages.md`. `CHANGELOG.md` `[Unreleased]` Added entry. `en` catalog regenerated | Docs check green on the branch |

## US-2 — Reading any version at its own address (P1, issue #61)

| Id | Task | Done when |
|---|---|---|
| T008 | `tests/test_models.py::TestWithReplacedAt` — for three published versions of one document and one of another, `replaced_at` is the next version's `published_at` of the same document, and `None` for each document's current version; drafts do not count as replacements | Fails before T009 |
| T009 | `mvp_compliance/models.py` — `VersionQuerySet.with_replaced_at()` per plan.md *Querysets* | T008 passes |
| T010 | `tests/test_urls.py::TestVersionNumberConverter` — `2026.1` and `2026.12` reverse and resolve to the `version` pattern; `versions`, `2026`, `26.1`, `2026.x` do not resolve to it | Fails before T011 |
| T011 | `tests/test_views.py::TestVersionView` — anonymous GET of a superseded version's address is 200 with its stored `html`, says it was replaced, shows both dates, links to the document's page; the current version's address says it is in force and shows the same wording as the document's page; after a later publication a version's address and wording are unchanged; a number belonging to another document is 404; a draft is unreachable; shell present; name escaped; query count constant (scenarios 1–7, FR-002, FR-005–FR-010, SC-002–SC-004, SC-006) | Fails before T012 |
| T012 | `urls.py` — `VersionNumberConverter` and the `version` pattern; `views.py` — `VersionView`; `templates/mvp_compliance/version_detail.html` | T010 and T011 pass |
| T013 | `docs/pages.md`: the `version` URL name and its arguments, what a version's page shows in force and superseded. `docs/models.md`: `with_replaced_at()`. `en` catalog regenerated | Docs check green on the branch |

## US-3 — Finding a document and its earlier versions (P2, issue #62)

| Id | Task | Done when |
|---|---|---|
| T014 | `tests/test_views.py::TestVersionListView` — every published version newest first with number, in-force date and replaced date, the current one marked, each linking to its page, no drafts, 404 for a draft-only document, shell present, query count constant for 2 and 6 versions (scenarios 2–4, 7, FR-003, SC-006). `TestDocumentView` gains: the document's page links to its version list (scenario 1, FR-011) | Fails before T015 |
| T015 | `urls.py` `versions` pattern; `views.py` `VersionListView`; `templates/mvp_compliance/version_list.html`; the "Earlier versions" link in `document_detail.html` | T014 passes |
| T016 | `tests/test_views.py::TestDocumentIndexView` — lists every document with a version in force, alphabetically, each linking to its page, and no draft-only or empty document; with nothing in force says nothing has been published yet; shell present; query count constant for 2 and 6 documents (scenarios 5–7, FR-004, SC-006) | Fails before T017 |
| T017 | `urls.py` `index` pattern; `views.py` `DocumentIndexView`; `templates/mvp_compliance/document_index.html`; breadcrumbs on all four views link the index (tests assert the crumb hrefs reverse to `mvp_compliance:index` and `mvp_compliance:document`). `demo/menus.py` gains a "Legal documents" `AppMenu` entry pointing at the index | T016 passes |
| T018 | `docs/pages.md`: the `versions` and `index` URL names, the "Legal" footer link to the index. `en` catalog regenerated | Docs check green on the branch |

## US-4 — A stable address, and a project's own presentation (P2, issue #63)

| Id | Task | Done when |
|---|---|---|
| T019 | `tests/test_models.py::TestDocumentSlug` — a slug with capitals, underscores or a leading hyphen fails `full_clean()`; before any publication the slug can be changed through `save()` and `update()`; after one version is published, changing it through `save()`, `update(slug=...)` and `bulk_update(..., ["slug"])` raises `PublishedVersionError`, and the stored slug is unchanged; the name can still be changed and the pages keep their addresses and show the new name (scenarios 2–5, FR-015, FR-017, SC-005) | Fails before T020 |
| T020 | `mvp_compliance/models.py` — the lowercase validator, `Document.save()` and `DocumentQuerySet.update()` guards per plan.md *Freezing*. Migration updated in place (branch-local: fold the validator into `0007`) | T019 passes; `makemigrations --check` clean |
| T021 | `tests/test_admin.py` — the add form prepopulates `slug` from `name`; the change form of a document with no published version lets the slug be edited; the change form of a document with a published version shows the slug read-only, renders without error, and a POST carrying a different slug leaves it unchanged (scenarios 1–3, FR-016, FR-017) | Fails before T022 |
| T022 | `mvp_compliance/admin.py` — `prepopulated_fields`, `get_readonly_fields`, `get_prepopulated_fields` per plan.md *Admin* | T021 passes |
| T023 | `tests/test_views.py::TestTemplateOverride` — with a test-only templates directory containing `mvp_compliance/document_detail.html`, the document's page renders the override; same for one other page template (scenario 6, FR-018). `tests/test_views.py` — no page adds a menu entry: the `AppMenu` children are the same before and after `mvp_compliance.urls` is imported (FR-014) | Passes |
| T024 | `docs/pages.md`: every template path with the context it receives and how to override one. `docs/models.md`: when the slug is fixed and the error raised. `docs/authoring.md`: choosing the slug, fixed at first publication. `CONTEXT.md` **Slug** (FR-022). `CHANGELOG.md` Added entry for the slug. `en` catalog regenerated, with a test that every string in the four page templates is in it (SC-008). The test-only `de` catalog under `tests/locale/` gains the version page's in-force sentence, and a test renders that page under `translation.override("de")` and finds it (FR-019). The same test module asserts that the rendered text of the four pages and the msgids of their strings contain none of "compliant", "compliance" or "GDPR" (FR-020) | Docs check green; catalog test passes |
